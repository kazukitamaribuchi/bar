name: Docker Image CI

on:
  push:
    branches: [ "master" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - name: yarn build
        run: | #
          ls
          cd web
          docker build .
          yarn build
      - name: move files
        run: | #
          cd web
          ls
      - name: collect static
        run: | #
          cd api
          ls
      - name: heroku release
        run : | #
          ls
